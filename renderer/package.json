{
  "name": "auto-video-factory-render",
  "version": "1.0.0",
  "private": true,
  "description": "Render TikTok/Reels video scenes from a JSON timeline with ffmpeg.",
  "type": "commonjs",
  "scripts": {
    "render": "node scripts/render.js",
    "render:shoe": "node scripts/render.js shoe_001",
    "sync-prompt": "node scripts/syncPromptEnums.js",
    "timeline": "node scripts/generateTimeline.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google/genai": "^2.12.0"
  }
}
